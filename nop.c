#include "monty.h"

/**
 * nop -  do not do anything nothing
 * @stack: passed from main, but gonna be voided
 * @line_number: also gonna be voided
 */

void nop(stack_t **stack __attribute__((unused)), unsigned int line_number __attribute__((unused)))
{
}
